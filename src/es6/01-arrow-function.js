function square(num) {
  return num * num;
}

const square = (num) => {
  return num * num;
};

const square = (num) => num * num; //una de las cosas que facilita la arrow function es que cuando una fuincion es simple no se necesita poner {} ni return y se puede hacer mas coomprimido
